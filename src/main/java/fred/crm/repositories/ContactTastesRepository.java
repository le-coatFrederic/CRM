package fred.crm.repositories;

import fred.crm.models.ContactTastes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContactTastesRepository extends JpaRepository<ContactTastes, Long> {
}
